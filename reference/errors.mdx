---
title: Error reference
description: Error formats returned by the Atlas Relay API.
---

# Error reference

API errors contain a stable `code`, a human-readable `message`, and a `request_id`. Validation errors also include a `fields` map.

Log the request ID when escalating an issue to support. Do not expose project tokens, signing secrets, or event payloads in support requests.
